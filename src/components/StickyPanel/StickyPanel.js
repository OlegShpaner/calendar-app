import React from 'react';
import PropTypes from 'prop-types';
import './StickyPanel.css';

const StickyPanel = () => (
  <div className="StickyPanel">
    StickyPanel Component
  </div>
);

StickyPanel.propTypes = {};

StickyPanel.defaultProps = {};

export default StickyPanel;
